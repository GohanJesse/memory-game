// Mélange de carte
// import { CardType } from "../models/CardType";

// export const shuffleCards = (cardsArray: CardType[]): CardType[] => {
//   const shuffled = [...cardsArray, ...cardsArray]
//     .sort(() => Math.random() - 0.5)
//     .map((card, index) => ({ ...card, id: index }));
//   return shuffled;
// };

// export const initializeGame = (cardsArray: CardType[]): CardType[] => {
//   return shuffleCards(cardsArray);
// };
